export default {
    name: 'pathToRect',
    description: 'Converts paths to rounded rectangles',
    fn: () => {
        return {
            element: {
                enter: (node, parent) => {
                    
                }
            }
        }
    }
}