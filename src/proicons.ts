import icons from './icons'

class ProIconInfo {
    constructor(key: string) {

    }
}
function replace(rootElm: Element): void {

}
function getIconInfo(key: string): ProIconInfo {
    return new ProIconInfo(key);
}