import * as React from 'react'

export function ProIcon({
    icon, color = 'currentColor', width = 1.5, outline = false, caps = 'round', joins = 'round'
}) {
    return (
        <p>Hello, world!</p>
    )
}